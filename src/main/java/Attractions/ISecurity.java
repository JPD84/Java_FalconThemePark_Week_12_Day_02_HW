package Attractions;

import Visitors.Visitor;

public interface ISecurity {

    public void checkVisitorAge(Visitor visitor) {
        if(PlayGround.maxAge()>(visitor.age)){
            this.(visitor.remove : add.visitor)();
        }

        public void checkVisitorHeight(Visitor visitor){
            if(RollerCoaster.maxHeight)
        }
}

//Wrtie similar function for the visitor height, if height is over 200 then double fee if not then normal fee.
//Implement this to the RollerCoaster file.
